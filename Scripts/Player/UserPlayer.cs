using UnityEngine;
using System.Collections;

public class UserPlayer : Player {

    public UserPlayer(string characterName, CharacterClass characterClass, Dragon dragon, int currentFieldIndex) : base(characterName, characterClass, dragon, currentFieldIndex)
    {
    }

    // Use this for initialization
    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
